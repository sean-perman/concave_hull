# Focused studies

- `concavity_monotonicity/` demonstrates that increasing *k* does not always
  make the resulting valid hull less concave.
- `validity_monotonicity/` demonstrates that fixed-*k* validity is not monotone.
- `interesting_cases/` contains screenshots retained from exploratory testing.

The executable studies use the maintained `concave_hull_experiment/` package.
